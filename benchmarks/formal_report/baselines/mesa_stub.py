"""Traditional ABM reference notes for the formal benchmark."""

from __future__ import annotations


def reference_notes() -> dict[str, str]:
    return {
        "status": "not-measured",
        "reason": "Mesa-style agent loops do not provide LLM memory recall without custom extensions.",
        "usage": "Use this stub only as a transparent capability boundary, not as a performance claim.",
    }
