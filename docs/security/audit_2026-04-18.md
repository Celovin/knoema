# Phase 34 Security Audit

Date: 2026-04-18

Mode: local daily audit, high-confidence findings only.

## Summary

No critical or high-severity vulnerabilities were found in the local audit.

| Check | Result |
| --- | --- |
| Bandit over `src/` | 0 findings |
| pip-audit over current Python environment | 0 vulnerabilities, 1 expected local-package skip |
| Safety check over current Python environment | 0 vulnerabilities |
| npm audit for `website/` | 0 vulnerabilities |
| npm audit for `sdk/typescript/` | 0 vulnerabilities |
| Secret pattern scan in public tree | No high-confidence secrets |
| Git history scan for common key formats | No high-confidence secrets |
| CycloneDX SBOM | Generated at `docs/security/knoema-sbom.cdx.json` |

## Tool Evidence

Bandit:

- Command: `bandit -r src -f json -o tmp/security/bandit.json`
- Result: 0 high, 0 medium, 0 low findings.
- Scanned LOC reported by Bandit: 2,758.

pip-audit:

- Command: `pip-audit -f json -o tmp/security/pip-audit.json`
- Result: 246 dependencies inspected, 0 vulnerabilities.
- Note: local editable package `knoema-engine==0.1.0` was skipped because it is not published on PyPI.

Safety:

- Command: `safety check --output json --save-json tmp/security/safety.json --exit-code`
- Result: 246 packages inspected, 0 vulnerabilities, 0 ignored vulnerabilities.
- Note: `safety check` is deprecated by Safety CLI. It was used because it still supports local JSON output without account authentication.

npm:

- `website/`: 58 dependencies, 0 vulnerabilities.
- `sdk/typescript/`: 1 dependency set, 0 vulnerabilities.

SBOM:

- Command: `cyclonedx-py environment .venv --pyproject pyproject.toml --mc-type library --of JSON --output-reproducible -o docs/security/knoema-sbom.cdx.json`
- CycloneDX spec: 1.6.
- Components: 245.
- Scope note: this is an environment SBOM, so it includes audit tooling installed in the local virtual environment as well as project dependencies.

## Findings

| Severity | Finding | Status |
| --- | --- | --- |
| Critical | None | Not found |
| High | None | Not found |
| Medium | None meeting the daily confidence gate | Not reported |

## Operational Actions

These are not application vulnerabilities, but they should be closed before public launch or a serious research submission.

| Priority | Item | Evidence | Action |
| --- | --- | --- | --- |
| P1 | GitHub secret scanning is disabled | `gh api repos/Celovin/knoema` reported `secret_scanning.status = disabled` | Enable secret scanning and push protection in repository settings. |
| P1 | Dependabot security updates are disabled | same API response reported `dependabot_security_updates.status = disabled` | Enable Dependabot security updates in repository settings. |
| P2 | Environment SBOM includes local audit tools | SBOM generated from `.venv` after installing Bandit, pip-audit, Safety, and CycloneDX | For release artifacts, regenerate SBOM from a clean runtime-only environment. |

## STRIDE Snapshot

| Component | Primary Risk | Current Control |
| --- | --- | --- |
| CLI and config loader | malformed YAML or unsafe scenario input | Pydantic validation and fuzz tests |
| LLM clients | user API key handling and provider output variability | API keys are constructor/session inputs; deterministic local mode remains test path |
| Playground | accidental API key storage | UI password field and session-only API key path |
| Scenario DSL | unsafe public-safety use | ethics validator and synthetic-only documentation boundary |
| GitHub Actions | dependency drift and workflow supply chain | pinned named actions with Dependabot config added |

## Data Classification

| Class | Data |
| --- | --- |
| Restricted | user-supplied LLM API keys, if entered into local playground sessions |
| Confidential | unpublished planning documents under ignored `planning/` |
| Internal | local audit outputs under ignored `tmp/` |
| Public | source code, examples, synthetic logs, docs, benchmark reports, SBOM |

## Disclaimer

This is an AI-assisted first-pass audit, not a substitute for a professional penetration test. For production systems handling sensitive data, payments, or personally identifiable information, use a qualified security firm in addition to these checks.
